class Reciter {
  final String id;
  final String name;
  final String imageUrl;

  const Reciter({required this.id, required this.name, required this.imageUrl});
}

// Placeholder list of reciters. Replace with real data if available.
const List<Reciter> quranReciters = <Reciter>[];
